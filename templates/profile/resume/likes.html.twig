{% extends 'layouts/account_layout.html.twig' %}

{% block title %}Likes{% endblock %}

{% set currentTab = 'resume_likes' %}

{% block content %}
    <h2 class="title title-second text-tertiary-dark">Likes</h2>
    <div class="w-full grid lg:grid-cols-3 md:grid-cols-2 grid-cols-1 gap-2">
        {% for post in posts %}
            {% include "partials/_cardPostMin.html.twig" %}
        {% endfor %}
    </div>
    {% include "partials/_pagination.html.twig" with {
        'route': 'resume_likes',
        'parameters': {},
        'page': currentPage,
        'pages': totalPages
    } %}
{% endblock %}
