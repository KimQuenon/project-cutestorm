{% extends 'profile/conversations/base.html.twig' %}

{% block title %}New Conversation{% endblock %}
{% block subtitle %}New Conversation with {{otherUser.pseudo}}{% endblock %}

{% block message_content %}
    <p>Envoyez un premier message pour démarrer la conversation :</p>
{% endblock %}

{% block form_content %}
    {{ form_start(form) }}
    <div class="d-flex align-items-center justify-content-between">
        <div class="input-group" style="width: 90%;">
            {{ form_widget(form.content, {'attr': {'class': 'form-control form-control-sm', 'style': 'padding: 10px;'}}) }}
        </div>
        <div>
            <button type="submit" class="btn btn-primary p-3">
                <i class="fas fa-arrow-up"></i> Envoyer
            </button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
