{% extends 'base.html.twig' %}

{% block title %}Messagerie{% endblock %}

{% block body %}
    <div class="container full">
        <a href="{{path('contact_index')}}" type="button" class="btn btn-secondary">Back</a>
        <h1>Message from {{contact.name}}</h1>
        <p>{{contact.message}}</p>
        <a href="mailto:{{contact.email}}" type="button" class="btn btn-primary">Répondre</a>
        <a href="{{path('contact_delete', {'id':contact.id})}}" type="button" class="btn btn-danger">Supprimer</a>
    </div>
{% endblock %}