{% extends 'base.html.twig' %}

{% block title %}Connexion Admin{% endblock %}

{% block body %}
    <div class="container full">
        <div class="row">
            <div class="col-md-6 offset-md-3 bg-light p-3">
                <h1>Connexion</h1>
                {% if hasError %}
                    <div class="alert alert-danger">
                        Une erreur est survenue, le nom d'utilisateur ou le mot de passe est invalide.
                    </div>
                {% endif %}
                {% if loginError %}
                    <div class="alert alert-danger">
                        {{loginError}}
                    </div>
                {% endif %}
                <form method="POST">
                    <div class="form-group my-3"><label for="username">Email</label><input type="text" class="form-control" placeholder="Adresse email..." name="_username" id="username" value={{username}}></div>
                    <div class="form-group my-3"><label for="password">Password</label><input type="password" class="form-control" placeholder="Mot de passe..." name="_password" id="password"></div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary my-3"><i class="fas fa-lock-open"></i> Connexion</button>
                        <a href="{{ path('homepage')}}" class="btn btn-link">
                            <i class="fas fa-arrow-circle-left"></i>
                            Retour au site
                        </a>
                    </div>
                </form>
            </div>
        </div>

    </div>
{% endblock %}